services:
  mongo:
    image: mongo:7
    container_name: reservation-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: reservations
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
